import { FormBuilder } from "@angular/forms";
import * as moment from 'moment';

export class ExcelSheet {
  employeeName: string;
  absenceId: string;
  reason: string;
  districtName: string;
  schoolName: string;
}
